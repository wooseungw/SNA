<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cool Button</title>
    <style>
        .my-cool-button {
            background-color: #3fbf74; /* 기본 버튼 색상 */
            color: white; /* 텍스트 색상 */
            padding: 15px 30px; /* 버튼 내부 패딩 */
            font-size: 16px; /* 텍스트 크기 */
            border: none; /* 테두리 없애기 */
            border-radius: 8px; /* 둥근 모서리 */
            cursor: pointer; /* 클릭 가능한 커서 */
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3); /* 그림자 효과 */
            transition: all 0.3s ease; /* 부드러운 애니메이션 */
        }

        .my-cool-button:hover {
            background-color: #45a049; /* 호버 시 배경 색상 변경 */
            transform: translateY(-3px); /* 살짝 위로 올라가는 효과 */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4); /* 호버 시 그림자 확장 */
        }

        .my-cool-button:active {
            background-color: #3e8e41; /* 클릭 시 배경 색상 변경 */
            transform: translateY(2px); /* 클릭 시 살짝 눌리는 효과 */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* 클릭 시 그림자 축소 */
        }
    </style>
</head>
<body>

<button class="my-cool-button">{{.}}</button>

</body>
</html>